{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "MCP server configuration. This file can be committed to share MCP servers with your team.",
  "mcpServers": {
    "filesystem": {
      "name": "filesystem",
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/mcp-server-filesystem",
        "."
      ],
      "enabled": false,
      "description": "File system access MCP server"
    },
    "github": {
      "name": "github",
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/mcp-github"
      ],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      },
      "enabled": false,
      "description": "GitHub integration MCP server"
    },
    "code-explorer": {
      "name": "code-explorer",
      "type": "stdio",
      "command": "/home/patrice/DEV/gitnexus-rs/target/release/gitnexus",
      "args": ["mcp"],
      "enabled": true,
      "description": "Code Explorer (gitnexus binary) — graph-powered code intelligence: query, impact/blast-radius, callers, hotspots, complexity over a pre-indexed knowledge graph. Canonical name `code-explorer` so it de-dups with a user-level entry of the same name (avoids loading 31 tools twice). Local path /home/patrice/DEV/gitnexus-rs/target/release/gitnexus; /home/patrice/code-buddy/src is indexed. https://github.com/phuetz/code-explorer"
    },
    "lm-resizer": {
      "name": "lm-resizer",
      "type": "stdio",
      "command": "lm-resizer",
      "args": ["mcp"],
      "enabled": false,
      "description": "lm-resizer — context compression for agents (compress noisy tool output, retrieve originals by hash, stats). Build https://github.com/phuetz/lm-resizer and enable. Code Buddy also auto-compresses tool output before the LLM when CODEBUDDY_LM_RESIZER=true."
    }
  }
}
